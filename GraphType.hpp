//hagitdahan101@gmail.com
//315158568
#ifndef GRAPHTYPE_HPP
#define GRAPHTYPE_HPP
// Enum to represent the type of graph
enum class GraphType {
    DIRECTED,UNDIRECTED};
// Enum to represent whether the graph has weights on its edges
enum class WeightType {
    UNWEIGHTED,WEIGHTED };
// Enum to represent whether the graph allows negative edges
enum class EdgeType {
    NON_NEGATIVE,ALLOW_NEGATIVE};

#endif